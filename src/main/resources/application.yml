server:
  servlet.contextPath: /api
  port: ${port:9200}

spring:
  data:
    mongodb:
      host: ${MONGO_HOST:localhost}
      port: ${MONGO_PORT:27017}
      username: ${MONGO_USER:testUser}
      password: ${MONGO_PASSWORD:testPassword}
      database: ${MONGO_DATABASE:testDatabase}
